
apiVersion: v1
kind: Service
metadata:
  name: ledger-events-app-service
  labels:
    com.job.service: ledger-events-app-service
spec:
  type: LoadBalancer
  ports:
    - port: 5001
      targetPort: 5001
      nodePort: 31001
  selector:
    com.job.service: ledger-events-app
    tier: golang